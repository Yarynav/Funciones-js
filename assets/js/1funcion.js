/**
 *
 * @param {number} a
 * @param {number} b
 * @param {number} c
 * @returns {number}
 *@description ejemplo de cambio de una funcion tradicional por una funcion de expresion
 */

function example(a, b, c) {
  return a + b + c;
}

const example = function (a, b, c) {
  return a + b + c;
};
